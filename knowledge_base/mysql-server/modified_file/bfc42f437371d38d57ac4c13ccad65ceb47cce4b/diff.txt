diff --git a/sql/range_optimizer/range_analysis.cc b/sql/range_optimizer/range_analysis.cc
index a9c51cdb2a7..2b843871842 100644
--- a/sql/range_optimizer/range_analysis.cc
+++ b/sql/range_optimizer/range_analysis.cc
@@ -424,7 +424,7 @@ static SEL_TREE *get_func_mm_tree_from_in_predicate(
         */
         if (and_tree == nullptr) return nullptr;
       }
-      or_tree = tree_or(param, remove_jump_scans, and_tree, or_tree);
+      or_tree = tree_or(param, remove_jump_scans, or_tree, and_tree);
     }
     return or_tree;
   }
