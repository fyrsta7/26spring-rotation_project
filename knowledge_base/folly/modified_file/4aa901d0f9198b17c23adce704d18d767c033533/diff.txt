diff --git a/folly/io/Cursor.h b/folly/io/Cursor.h
index 3cf369e9e..a13092394 100644
--- a/folly/io/Cursor.h
+++ b/folly/io/Cursor.h
@@ -913,6 +913,10 @@ class QueueAppender : public detail::Writable<QueueAppender> {
     }
   }
 
+  void insert(const folly::IOBuf& buf) {
+    insert(buf.clone());
+  }
+
  private:
   folly::IOBufQueue* queue_;
   size_t growth_;
