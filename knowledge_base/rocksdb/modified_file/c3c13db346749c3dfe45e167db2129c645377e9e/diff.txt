diff --git a/db/memtable.cc b/db/memtable.cc
index 097b60ff5..7a7c6bb7a 100644
--- a/db/memtable.cc
+++ b/db/memtable.cc
@@ -185,6 +185,9 @@ bool MemTable::Get(const LookupKey& key, std::string* value, Status* s,
           return true;
         }
       }
+    } else {
+      // exit loop if user key does not match
+      break;
     }
   }
 
