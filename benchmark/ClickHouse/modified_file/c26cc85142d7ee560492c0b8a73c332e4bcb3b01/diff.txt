diff --git a/src/Processors/Executors/PipelineExecutor.cpp b/src/Processors/Executors/PipelineExecutor.cpp
index e61443dcfad..2d214f0e2a1 100644
--- a/src/Processors/Executors/PipelineExecutor.cpp
+++ b/src/Processors/Executors/PipelineExecutor.cpp
@@ -526,9 +526,13 @@ void PipelineExecutor::finalizeExecution()
         return;
 
     bool all_processors_finished = true;
-    for (auto & node : graph)
-        if (node.status != ExecStatus::Finished)  /// Single thread, do not hold mutex
-            all_processors_finished = false;
+
+    for (auto & node : graph) {
+	    if (node.status != ExecStatus::Finished)  { /// Single thread, do not hold mutex
+		    all_processors_finished = false;
+		    break;
+	    }
+    }
 
     if (!all_processors_finished)
         throw Exception("Pipeline stuck. Current state:\n" + dumpPipeline(), ErrorCodes::LOGICAL_ERROR);
